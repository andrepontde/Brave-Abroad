POSTGRES_DB=brave_abroad
POSTGRES_USER=brave_abroad
POSTGRES_PASSWORD=change_this_password

# The app runs inside Compose, so PostgreSQL is reached through the service name.
DATABASE_URL=postgresql+psycopg://brave_abroad:change_this_password@db:5432/brave_abroad
