services:
  database:
    image: postgres:16
    environment:
      POSTGRES_DB: brave_abroad
      POSTGRES_USER: brave_abroad
      POSTGRES_PASSWORD: local_password
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: